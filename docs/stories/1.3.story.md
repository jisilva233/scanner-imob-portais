---
id: "1.3"
title: "Painel Streamlit com Filtros de Preço, Bairro e Metragem"
status: Ready for Review
epic: "1"
story_type: Frontend
complexity: Low
author: River (SM)
created_at: "2026-03-13"
---

# Story 1.3 — Dashboard Streamlit

## Story

**Como** usuário do scanner imobiliário,
**quero** abrir um painel web com `streamlit run dashboard/app.py` e filtrar anúncios por preço, bairro e metragem,
**para que** eu possa analisar o mercado imobiliário sem precisar escrever queries SQL.

## Status

Ready for Review

## Acceptance Criteria

- [x] **AC1:** `streamlit run dashboard/app.py` abre painel em `http://localhost:8501`
- [x] **AC2:** Painel exibe métricas: total de anúncios, preço médio, número de portais, número de bairros
- [x] **AC3:** Filtro de preço: slider de intervalo `(min, max)` formatado como `R$ X.XXX`
- [x] **AC4:** Filtro de bairro: `multiselect` com todos os bairros únicos disponíveis
- [x] **AC5:** Filtro de metragem: slider de intervalo `(min, max)` formatado como `XX m²`
- [x] **AC6:** Filtro de portal: `multiselect` com ZAP / VIVAREAL / OLX
- [x] **AC7:** Tabela exibe anúncios filtrados com coluna "Link" clicável
- [x] **AC8:** Dados carregados via SQLAlchemy com `@st.cache_data(ttl=300)` (cache 5 min)
- [x] **AC9:** Mensagem de aviso quando não há dados (`st.warning`)
- [x] **AC10:** Layout usa `st.set_page_config(layout="wide")`

## Scope

**IN:**
- `dashboard/__init__.py`
- `dashboard/app.py` — aplicação Streamlit completa

**OUT:**
- Gráficos ou visualizações avançadas (fora do escopo desta story)
- Autenticação ou controle de acesso
- Deploy em servidor (uso local apenas)

## Dependencies

- **Story 1.1 deve estar Done** — `db/models.py` e engine disponíveis
- **Story 1.2 deve estar Done** — tabela populada com dados reais para testar filtros

## Dev Notes

### Estrutura do `app.py`
[Source: squads/imob-scraper/tasks/run-dashboard.md]

```python
import streamlit as st
import pandas as pd
from sqlalchemy import select
from sqlalchemy.orm import Session
from db.models import PropertyListingDB, engine

st.set_page_config(page_title="Imob Scanner", page_icon="🏠", layout="wide")
```

### Carregamento de Dados com Cache
[Source: squads/imob-scraper/agents/dashboard-agent.md]
```python
@st.cache_data(ttl=300)
def load_data() -> pd.DataFrame:
    with Session(engine) as session:
        rows = session.execute(
            select(PropertyListingDB).where(PropertyListingDB.is_active == True)
        ).scalars().all()
    return pd.DataFrame([...])
```

### Filtros — Sidebar
[Source: squads/imob-scraper/tasks/run-dashboard.md]

| Filtro | Widget | Config |
|--------|--------|--------|
| Preço (R$) | `st.sidebar.slider` | `format="R$ %,.0f"` |
| Bairro | `st.sidebar.multiselect` | `unique()` dos dados |
| Metragem (m²) | `st.sidebar.slider` | `format="%.0f m²"` |
| Portal | `st.sidebar.multiselect` | ZAP/VIVAREAL/OLX |

### Tabela com Link Clicável
[Source: squads/imob-scraper/tasks/run-dashboard.md]
```python
st.dataframe(
    filtered,
    column_config={
        "Link": st.column_config.LinkColumn("Link", display_text="Ver anúncio"),
        "Preço (R$)": st.column_config.NumberColumn(format="R$ %,.0f"),
    }
)
```

### Métricas de Resumo
```python
col1, col2, col3, col4 = st.columns(4)
col1.metric("Total de Anúncios", len(df))
col2.metric("Preço Médio", f"R$ {df['Preço (R$)'].mean():,.0f}")
col3.metric("Portais", df["Portal"].nunique())
col4.metric("Bairros", df["Bairro"].nunique())
```

### Padrões de Código
[Source: squads/imob-scraper/config/coding-standards.md]
- Nunca expor `DATABASE_URL` no código — usar `python-dotenv`
- Usar `@st.cache_data` para evitar reload a cada interação
- Colunas do DataFrame em português para exibição

## Tasks / Subtasks

- [x] **Task 1 (AC1, AC10):** Criar `dashboard/__init__.py` e `dashboard/app.py` com configuração base
  - [x] `st.set_page_config(layout="wide")`
  - [x] Título e ícone da página

- [x] **Task 2 (AC8, AC9):** Implementar `load_data()` com cache
  - [x] Query SQLAlchemy com filtro `is_active == True`
  - [x] Retorno como `pd.DataFrame` com colunas em português
  - [x] `st.warning` + `st.stop()` quando DataFrame vazio

- [x] **Task 3 (AC2):** Implementar bloco de métricas (4 colunas)
  - [x] Total, preço médio, portais, bairros

- [x] **Task 4 (AC3, AC4, AC5, AC6):** Implementar filtros na sidebar
  - [x] Slider de preço com `min/max` dinâmicos dos dados
  - [x] Multiselect de bairro com opções únicas
  - [x] Slider de metragem com `min/max` dinâmicos
  - [x] Multiselect de portal (ZAP/VIVAREAL/OLX)

- [x] **Task 5 (AC7):** Implementar tabela filtrada com link clicável
  - [x] Aplicar todos os filtros com `&` mask
  - [x] `st.dataframe` com `column_config` para Link e Preço

- [x] **Task 6:** Testes manuais
  - [x] Painel abre sem erro com tabela vazia (`st.warning` visível)
  - [x] Com dados: métricas exibem valores corretos
  - [x] Slider de preço filtra corretamente
  - [x] Multiselect de bairro filtra corretamente
  - [x] Link abre o anúncio original no navegador

## 🤖 CodeRabbit Integration

**Story Type Analysis:**
- Primary Type: Frontend
- Secondary Type: —
- Complexity: Low (arquivo único, UI simples)

**Specialized Agent Assignment:**
- Primary Agents: @dev, @imobScraper:dashboard-agent
- Supporting: @qa

**Quality Gate Tasks:**
- [x] Pre-Commit (@dev): `coderabbit --prompt-only -t uncommitted`
- [ ] Pre-PR (@devops): `coderabbit --prompt-only --base main`

**Focus Areas:**
- Cache: `@st.cache_data` presente
- Credenciais: `DATABASE_URL` via `.env`, nunca hardcoded
- UX: `st.warning` quando sem dados
- Performance: sem queries fora do `load_data()`

**Self-Healing:**
- Mode: light | Max Iterations: 2 | Severity: CRITICAL only

## Dev Agent Record

### Agent Model Used
claude-sonnet-4-6

### Debug Log
- python -m pytest: 31/31 testes passando (regressão completa com Stories 1.1 e 1.2)

### Completion Notes
- `dashboard/app.py` com todas as 10 ACs implementadas
- Cache de 5 min via @st.cache_data(ttl=300)
- Filtros sidebar: slider preço, multiselect bairro, slider metragem, multiselect portal
- Tabela com LinkColumn clicável e formatação de preço/data
- st.warning + st.stop() quando sem dados

### File List
- `dashboard/__init__.py` — já existia (Story 1.1)
- `dashboard/app.py` — criado

### Change Log
| Data | Autor | Mudança |
|------|-------|---------|
| 2026-03-13 | River (SM) | Story criada |
| 2026-03-13 | Dex (Dev) | Implementação completa — 31/31 testes passando |
