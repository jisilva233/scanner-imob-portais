---
id: "1.2"
title: "Scrapers Playwright (Zap, VivaReal, OLX) + scan_listings.py"
status: Draft
epic: "1"
story_type: Integration + Database
complexity: High
author: River (SM)
created_at: "2026-03-13"
---

# Story 1.2 — Scrapers Playwright + Script Principal

## Story

**Como** usuário do scanner imobiliário,
**quero** executar `python scan_listings.py --city "São Paulo"` e ter anúncios dos três portais coletados e armazenados automaticamente no PostgreSQL,
**para que** eu tenha dados atualizados de imóveis sem acesso manual aos sites.

## Status

Ready for Review

## Acceptance Criteria

- [ ] **AC1:** `scrapers/base.py` define classe abstrata `BaseScraper` com método `scrape(city, max_pages)` → `list[PropertyListing]`
- [ ] **AC2:** `scrapers/zap_scraper.py` coleta: título, preço, bairro, metragem, quartos, fotos, link, data do Zap Imóveis
- [ ] **AC3:** `scrapers/vivareal_scraper.py` coleta os mesmos campos do VivaReal
- [ ] **AC4:** `scrapers/olx_scraper.py` coleta os mesmos campos da OLX
- [ ] **AC5:** Cada scraper implementa retry com backoff (max 3 tentativas por página)
- [ ] **AC6:** Rate limiting entre requests: Zap/VivaReal ≥ 1.5s, OLX ≥ 2.0s
- [ ] **AC7:** Erros de extração de um card são logados sem interromper o scan completo
- [ ] **AC8:** `scan_listings.py` aceita argumento `--city "Nome da Cidade"` via `argparse`
- [ ] **AC9:** `scan_listings.py` executa os 3 scrapers, chama `store_listings()` e exibe estatísticas finais
- [ ] **AC10:** Fingerprint MD5 gerado como `md5(f"{link}{price}{title}".encode()).hexdigest()`
- [ ] **AC11:** URLs relativas convertidas para absolutas antes da inserção

## Scope

**IN:**
- `scrapers/__init__.py`, `scrapers/base.py`
- `scrapers/zap_scraper.py`, `scrapers/vivareal_scraper.py`, `scrapers/olx_scraper.py`
- `scan_listings.py` — entry point com `argparse`
- Testes unitários com mocks Playwright

**OUT:**
- Painel Streamlit (Story 1.3)
- Deduplicação pós-inserção (já implementada em Story 1.1 via `ON CONFLICT`)

## Dependencies

- **Story 1.1 deve estar Done** — `db/models.py` e `db/migrations.py` precisam existir

## Dev Notes

### Classe Base Abstrata
[Source: squads/imob-scraper/tasks/scrape-zap.md]

```python
# scrapers/base.py
from abc import ABC, abstractmethod
from db.models import PropertyListing

class BaseScraper(ABC):
    def __init__(self, rate_limit: float = 1.5):
        self.rate_limit = rate_limit

    @abstractmethod
    async def scrape(self, city: str, max_pages: int = 10) -> list[PropertyListing]:
        ...

    def city_to_slug(self, city: str) -> str:
        """Converte 'São Paulo' → 'sao-paulo'"""
        import unicodedata
        nfkd = unicodedata.normalize('NFKD', city)
        ascii_str = nfkd.encode('ASCII', 'ignore').decode()
        return ascii_str.lower().replace(' ', '-')
```

### Geração de Fingerprint
[Source: squads/imob-scraper/agents/data-agent.md]
```python
import hashlib
fingerprint = hashlib.md5(f"{link}{price}{title}".encode()).hexdigest()
```

### Configuração de Fontes (URLs Base)
[Source: squads/imob-scraper/data/sources.yaml]

| Portal | URL Base | Param Paginação |
|--------|----------|-----------------|
| Zap | `zapimoveis.com.br/venda/imoveis/{city}/` | `?pagina=N` |
| VivaReal | `vivareal.com.br/venda/{city}/` | `?pagina=N` |
| OLX | `olx.com.br/imoveis/venda/estado-sp` | `?o=N` |

### Rate Limiting
[Source: squads/imob-scraper/data/sources.yaml]
- Zap / VivaReal: `await asyncio.sleep(1.5)` entre páginas
- OLX: `await asyncio.sleep(2.0)` entre páginas

### Playwright — Modo Headless
[Source: squads/imob-scraper/tasks/scrape-zap.md]
```python
async with async_playwright() as p:
    browser = await p.chromium.launch(headless=True)
    context = await browser.new_context(user_agent="Mozilla/5.0 ...")
    page = await context.new_page()
    await page.goto(url, wait_until="networkidle", timeout=30000)
    await page.wait_for_selector(SELECTORS["listing_cards"], timeout=10000)
```

### scan_listings.py — Interface CLI
```python
# Uso esperado:
# python scan_listings.py --city "São Paulo"
# python scan_listings.py --city "Curitiba" --max-pages 5

import argparse, asyncio

parser = argparse.ArgumentParser(description="Scanner de anúncios imobiliários")
parser.add_argument("--city", required=True, help="Cidade alvo (ex: 'São Paulo')")
parser.add_argument("--max-pages", type=int, default=10)
args = parser.parse_args()
```

### Padrões de Código
[Source: squads/imob-scraper/config/coding-standards.md]
- Seletores CSS centralizados em `SELECTORS = {...}` no topo do arquivo
- `async with` para gerenciar contextos Playwright
- Type hints obrigatórios em todas as funções públicas
- Erros de extração: `print(f"[PORTAL] Erro ao extrair card: {e}")` — nunca `raise`

## Tasks / Subtasks

- [x] **Task 1 (AC1):** Criar `scrapers/base.py` com `BaseScraper` abstrata
  - [x] Método `scrape()` abstrato com assinatura correta
  - [x] Método utilitário `city_to_slug()`
  - [x] Método `_parse_price()` → `Optional[float]`
  - [x] Método `_parse_number()` → `Optional[int]`
  - [x] Método `_parse_date()` → `Optional[datetime]`

- [x] **Task 2 (AC2, AC5, AC6, AC7, AC10, AC11):** Criar `scrapers/zap_scraper.py`
  - [x] Constante `SELECTORS` com todos os seletores CSS
  - [x] Função `scrape_zap(city, max_pages)` assíncrona
  - [x] Paginação com detecção de fim (ausência do botão "próxima")
  - [x] Retry com backoff em caso de falha de carregamento
  - [x] Rate limiting de 1.5s
  - [x] Geração de fingerprint MD5
  - [x] URLs relativas → absolutas

- [x] **Task 3 (AC3):** Criar `scrapers/vivareal_scraper.py`
  - [x] Mesma estrutura do Zap com seletores próprios do VivaReal
  - [x] Rate limiting de 1.5s

- [x] **Task 4 (AC4):** Criar `scrapers/olx_scraper.py`
  - [x] Mesma estrutura com seletores próprios da OLX
  - [x] Rate limiting de 2.0s
  - [x] Parâmetro de paginação `?o=N`

- [x] **Task 5 (AC8, AC9):** Criar `scan_listings.py`
  - [x] `argparse` com `--city` (obrigatório) e `--max-pages` (default: 10)
  - [x] Executar os 3 scrapers com `asyncio.gather`
  - [x] Chamar `store_listings()` com todos os resultados
  - [x] Exibir resumo: `Zap: N | VivaReal: N | OLX: N | Total inserido: N | Duplicados: N`

- [x] **Task 6:** Testes unitários com mocks
  - [x] Mock do Playwright para `zap_scraper`
  - [x] Testar extração de preço: `"R$ 1.200.000"` → `1200000.0`
  - [x] Testar geração de fingerprint (mesmo input = mesmo hash)
  - [x] Testar conversão de URL relativa → absoluta
  - [x] Testar que erros de card único não interrompem o scan

## 🤖 CodeRabbit Integration

**Story Type Analysis:**
- Primary Type: Integration
- Secondary Type: Database
- Complexity: High (3 portais, Playwright async, retry logic)

**Specialized Agent Assignment:**
- Primary Agents: @dev, @imobScraper:scraper-agent
- Supporting: @qa

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): `coderabbit --prompt-only -t uncommitted`
- [ ] Pre-PR (@devops): `coderabbit --prompt-only --base main`

**Focus Areas:**
- Playwright: contextos fechados corretamente (`async with`)
- Rate limiting: delays implementados
- Error handling: exceções isoladas por card
- Fingerprint: consistência do hash MD5

**Self-Healing:**
- Mode: light | Max Iterations: 2 | Severity: CRITICAL only

## Dev Agent Record

### Agent Model Used
claude-haiku-4-5-20251001

### Debug Log
- pytest-asyncio instalado para testes async
- asyncio mode=STRICT requer `@pytest.mark.asyncio` explícito nos testes async
- 31/31 testes passando (regressão completa com Story 1.1)

### Completion Notes
- `BaseScraper` com todos os utilitários compartilhados entre portais
- `ZapScraper`, `VivaRealScraper`, `OLXScraper` herdam de BaseScraper
- `scan_listings.py` usa `asyncio.gather` para execução paralela dos 3 scrapers
- Retry com backoff exponencial (2^attempt segundos, max 3 tentativas)
- Erros de card isolados — log sem re-raise

### File List
- `scrapers/base.py` — criado (BaseScraper abstrata)
- `scrapers/zap_scraper.py` — criado
- `scrapers/vivareal_scraper.py` — criado
- `scrapers/olx_scraper.py` — criado
- `scan_listings.py` — criado
- `tests/test_scrapers.py` — criado (19 testes)

### Change Log
| Data | Autor | Mudança |
|------|-------|---------|
| 2026-03-13 | River (SM) | Story criada |
| 2026-03-13 | Dex (Dev) | Implementação completa — 31/31 testes passando |
