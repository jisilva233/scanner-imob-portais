---
id: "1.1"
title: "Setup do Projeto e Camada de Dados (PostgreSQL)"
status: Draft
epic: "1"
story_type: Database + Integration
complexity: Medium
author: River (SM)
created_at: "2026-03-13"
---

# Story 1.1 — Setup do Projeto e Camada de Dados

## Story

**Como** desenvolvedor do scanner imobiliário,
**quero** configurar o projeto Python com todas as dependências, criar o schema PostgreSQL e implementar o modelo de dados,
**para que** os scrapers possam armazenar anúncios de forma estruturada com deduplicação.

## Status

Ready for Review

## Acceptance Criteria

- [ ] **AC1:** Repositório Python com estrutura de diretórios definida em `source-tree.md`
- [ ] **AC2:** `requirements.txt` contendo: `playwright`, `psycopg2-binary`, `sqlalchemy`, `streamlit`, `python-dotenv`, `pydantic`, `python-dateutil`
- [ ] **AC3:** `.env.example` com variável `DATABASE_URL`
- [ ] **AC4:** Tabela `property_listings` criada no PostgreSQL com todos os campos obrigatórios
- [ ] **AC5:** Coluna `fingerprint` (MD5 32 chars) com constraint `UNIQUE` — garante deduplicação
- [ ] **AC6:** Índices criados: `(city, neighborhood, price)` e `(source)`
- [ ] **AC7:** Modelo Pydantic `PropertyListing` valida todos os campos antes da inserção
- [ ] **AC8:** Função `store_listings()` usa `INSERT ... ON CONFLICT (fingerprint) DO NOTHING`
- [ ] **AC9:** `store_listings()` retorna dict `{inserted, duplicates, errors}`
- [ ] **AC10:** Script `db/migrations.py` cria o schema ao ser executado diretamente (`python db/migrations.py`)

## Scope

**IN:**
- Estrutura de diretórios do projeto
- `requirements.txt` e `.env.example`
- `db/models.py` — ORM SQLAlchemy + modelo Pydantic
- `db/migrations.py` — criação do schema
- Testes unitários do modelo e da função de inserção

**OUT:**
- Implementação dos scrapers (Story 1.2)
- Painel Streamlit (Story 1.3)
- Script `scan_listings.py` (Story 1.2)

## Dependencies

- Nenhuma story anterior
- PostgreSQL rodando localmente ou via Docker

## Dev Notes

### Stack Técnica
[Source: squads/imob-scraper/config/tech-stack.md]
- Python 3.11+
- Playwright >= 1.40.0 (instalação na Story 1.2)
- psycopg2-binary >= 2.9.0
- SQLAlchemy >= 2.0.0
- python-dotenv >= 1.0.0
- pydantic >= 2.0.0

### Schema da Tabela `property_listings`
[Source: squads/imob-scraper/tasks/store-listings.md]

| Coluna | Tipo | Constraints |
|--------|------|-------------|
| `id` | INTEGER | PK, AUTOINCREMENT |
| `fingerprint` | VARCHAR(32) | UNIQUE, NOT NULL, INDEX |
| `title` | VARCHAR(500) | NOT NULL |
| `price` | FLOAT | NULL |
| `neighborhood` | VARCHAR(200) | NULL |
| `area_sqm` | FLOAT | NULL |
| `bedrooms` | INTEGER | NULL |
| `photos_count` | INTEGER | NULL |
| `listing_url` | VARCHAR(1000) | NOT NULL |
| `listing_date` | DATETIME | NULL |
| `source` | VARCHAR(50) | NOT NULL (zap\|vivareal\|olx) |
| `city` | VARCHAR(100) | NOT NULL |
| `is_active` | BOOLEAN | NOT NULL, DEFAULT TRUE |
| `created_at` | DATETIME | NOT NULL, DEFAULT NOW() |

### Fingerprint (Deduplicação)
[Source: squads/imob-scraper/agents/data-agent.md]
```python
fingerprint = hashlib.md5(f"{link}{price}{title}".encode()).hexdigest()
```

### Estrutura de Diretórios
[Source: squads/imob-scraper/config/source-tree.md]
```
scanner-imob-portais/
├── scrapers/          # Story 1.2
├── db/
│   ├── __init__.py
│   ├── models.py      # Esta story
│   └── migrations.py  # Esta story
├── dashboard/         # Story 1.3
├── scan_listings.py   # Story 1.2
├── requirements.txt   # Esta story
└── .env.example       # Esta story
```

### Padrões de Código
[Source: squads/imob-scraper/config/coding-standards.md]
- Type hints obrigatórios em funções públicas
- Usar sessions SQLAlchemy com context manager
- Nunca escrever SQL raw — usar ORM
- Toda inserção deve verificar deduplicação antes

## Tasks / Subtasks

- [x] **Task 1 (AC1, AC2, AC3):** Criar estrutura de diretórios e arquivos base
  - [x] Criar diretórios: `scrapers/`, `db/`, `dashboard/`
  - [x] Criar `__init__.py` em cada diretório
  - [x] Criar `requirements.txt` com todas as dependências
  - [x] Criar `.env.example` com `DATABASE_URL=postgresql://user:password@localhost:5432/imob_scraper`

- [x] **Task 2 (AC4, AC5, AC6, AC7):** Implementar `db/models.py`
  - [x] Definir `PropertyListingDB` (ORM SQLAlchemy)
  - [x] Definir `PropertyListing` (modelo Pydantic para validação)
  - [x] Adicionar índices `ix_city_neighborhood_price` e `ix_source`
  - [x] Adicionar constraint `UNIQUE` em `fingerprint`

- [x] **Task 3 (AC8, AC9):** Implementar função `store_listings()` em `db/models.py`
  - [x] Implementar `INSERT ... ON CONFLICT (fingerprint) DO NOTHING`
  - [x] Retornar `{inserted, duplicates, errors}`
  - [x] Logar estatísticas de inserção

- [x] **Task 4 (AC10):** Implementar `db/migrations.py`
  - [x] Criar engine SQLAlchemy com `DATABASE_URL` do `.env`
  - [x] Executar `Base.metadata.create_all(engine)` para criar tabela
  - [x] Exibir confirmação de criação

- [x] **Task 5:** Escrever testes unitários
  - [x] Teste: `PropertyListing` valida campos obrigatórios
  - [x] Teste: `store_listings()` insere corretamente
  - [x] Teste: `store_listings()` ignora duplicatas (mesmo fingerprint)
  - [x] Teste: `store_listings()` retorna stats corretos

## 🤖 CodeRabbit Integration

**Story Type Analysis:**
- Primary Type: Database
- Secondary Type: Integration
- Complexity: Medium

**Specialized Agent Assignment:**
- Primary Agents: @dev, @data-agent
- Supporting: @qa

**Quality Gate Tasks:**
- [ ] Pre-Commit (@dev): `coderabbit --prompt-only -t uncommitted`
- [ ] Pre-PR (@devops): `coderabbit --prompt-only --base main`

**Focus Areas:**
- Schema compliance: constraints, indexes, tipos corretos
- Deduplicação: `ON CONFLICT DO NOTHING` funcionando
- Pydantic validation: campos obrigatórios validados
- Migration safety: idempotente (pode rodar múltiplas vezes)

**Self-Healing:**
- Mode: light | Max Iterations: 2 | Severity: CRITICAL only

## Dev Agent Record

### Agent Model Used
claude-haiku-4-5-20251001

### Debug Log
- pytest instalado via pip (não estava no PATH — python -m pytest usado)
- 12/12 testes passando em 1.28s

### Completion Notes
- `db/models.py` implementado com ORM + Pydantic + store_listings()
- `db/migrations.py` idempotente via `create_all()`
- `make_fingerprint()` extraído como função pública para reuso nos scrapers
- Testes usam mocks do SQLAlchemy Session (sem necessidade de DB real)

### File List
- `scrapers/__init__.py` — criado
- `db/__init__.py` — criado
- `dashboard/__init__.py` — criado
- `tests/__init__.py` — criado
- `requirements.txt` — criado
- `.env.example` — criado
- `db/models.py` — criado (PropertyListingDB, PropertyListing, make_fingerprint, store_listings)
- `db/migrations.py` — criado
- `tests/test_models.py` — criado (12 testes)

### Change Log
| Data | Autor | Mudança |
|------|-------|---------|
| 2026-03-13 | River (SM) | Story criada |
| 2026-03-13 | Dex (Dev) | Implementação completa — 12/12 testes passando |
